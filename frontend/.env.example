# 前端 API 基地址（与 admin 一致：不设或为空则用相对路径 + Vite 代理）
# 开发：不设置或留空，请求走 /api、/ai 等，由 vite.config.ts 代理到后端（如 http://localhost:8000）
# 生产：若前后端同域（Nginx 转发 /api、/ai 到后端），留空；若 API 在别域，设为完整地址，如 https://api.example.com
# VITE_API_BASE_URL=
